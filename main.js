// create Vue app
const app = Vue.createApp({
    // add app config options (object is mandatory)
    data () {
        // holds data to pass to the app
        return {
            heading: 'Test Header',
            paragraph: 'A new paragraph'
        }
    }
});
